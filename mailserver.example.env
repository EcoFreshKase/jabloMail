# Your primary email domain
DOMAIN=example.com

# Postmaster email address (receives system notifications)
POSTMASTER_ADDRESS=postmaster@example.com

# Enable SSL (Set to 1 if using Let's Encrypt or other SSL)
ENABLE_SSL=1
SSL_TYPE=letsencrypt

# Set mail users to be created (or add manually later)
ENABLE_SPAMASSASSIN=1       # Enable spam filtering
ENABLE_CLAMAV=1             # Enable antivirus scanning
ENABLE_FAIL2BAN=1           # Protect against brute-force attacks
ENABLE_POSTGREY=1           # Anti-spam greylisting

# DNS Blacklist (Block known spam sources)
ENABLE_DNSBL=1

# Authentication settings
ENABLE_SASLAUTHD=1
SASLAUTHD_MECHANISMS=pam

# Mail quota (uncomment and adjust if needed)
# MAILBOX_QUOTA=1000000000 # 1GB quota per mailbox

# Debugging
DMS_DEBUG=0 # Set to 1 to enable verbose logging for troubleshooting
